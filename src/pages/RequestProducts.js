import React from 'react';
import Navigation from '../components/Navigation';

const RequestProducts = () => {
  return (
    <div>
      <Navigation />
      <h1>Request Products</h1>
      {/* Implement the form to request products */}
    </div>
  );
};

export default RequestProducts;
